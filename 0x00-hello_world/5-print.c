#include <stdio.h>

/**
 *main-Entry point
 *printf-outputs formatted output
 *
 *Return: 0 to show (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
