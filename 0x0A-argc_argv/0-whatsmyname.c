#include <stdio.h>

/**
 * main - Funcction to print its name
 * @argc: command line arg
 * @argv: command line arg
 * Return: 0 to show SUCCESS
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
